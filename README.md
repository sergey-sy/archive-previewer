# archive-previewer

```
$ python app.py
 * Running on http://127.0.0.1:5000/
 * GET / example: curl http://localhost:5000
 * POST / example: curl --request POST -F "file=@/path/file/archive.zip" http://localhost:5000
```

```
$ pytest -v
 * test session starts
```